#ifndef CLEANSCREEN_H
#define CLEANSCREEN_H

void cleanScreen(); // Limpa a tela inteira (para menus)
void resetCursorPosition(); // Apenas move o cursor para o topo (para o loop do jogo)
void hideCursor();
void resetColor();
#endif